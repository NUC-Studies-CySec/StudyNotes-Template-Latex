
% put all the other packages here:
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{epigraph}
\usepackage{csquotes}
\usepackage{tikz}
\usepackage{xcolor}
\usepackage{svg}
\usepackage{float} % Enables you to force a "floating object" like the environments 'figure' and 'table' inline with the text.
\usepackage{morefloats} % Enalbes LaTeX to exceed the maximum 18 floats in a document.
\usepackage{nameref}
\usepackage{wrapfig}
\usepackage{subcaption} % Enable pictures to be aligned side by side. NOT COMPATIBLE with adjustbox.
\usepackage{adjustbox}

% packages for citation
\usepackage[style=apa]{biblatex} % The best typesetting implementation of APA that works with the rest of the other packages used in this style file.
\addbibresource{tex/MyCollection.bib} % Test bib file. used as example

% package for index and glossary
\usepackage{imakeidx}
% \usepackage{glossaries}
\usepackage[acronym]{glossaries}

% packages for typesetting source-code
\usepackage{listings} % Required for inserting code snippets
% \usepackage[usenames,dvipsnames]{xcolor} % Required for specifying custom colors and referring to colors by name !!! DO NOT USE THIS

% Improved table (tabular) width control. Enables use of max width and auto scale to wrap text.
% \usepackage{adjustbox}

% language specific package to properly manage special charaters:
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}

% font specific packages for defining custom fonts and encoding
\usepackage[T1]{fontenc}

% Packages to customize page layout
\usepackage{geometry}
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage{multicol}

% Optional packages
\usepackage{blindtext} % Sample text, false content, Lorem Ipsum type of random generated text.

